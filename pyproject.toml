[project]
name = "poker"
version = "0.1.0"
description = ""
authors = [
    {name = "TheGod0fWar",email = "moritz.sanner@gmx.de"}
]
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
]

[tool.poetry]
packages = [{include = "poker", from = "src"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
